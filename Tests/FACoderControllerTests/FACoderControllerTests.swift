import XCTest
@testable import FACoderController

final class FACoderControllerTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(FACoderController().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
